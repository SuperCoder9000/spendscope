# AI Agents Notes

The project explores lightweight AI-assisted product workflows using:

* AI-generated summaries
* recommendation generation
* product copy iteration
* debugging assistance

The application itself currently uses OpenAI APIs for personalized audit summary generation with graceful fallback handling.
